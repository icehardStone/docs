#!/bin/bash

# hostname              command             设置和显示系统的主机名称

hostname    -a          #显示主机别名
hostname    -d          #显示DNS域名
hostname    -f           #显示FQDN地址
hostname    -l           #显示主机IP地址
hostname    -s          #显示短主机名称
hostname    -y          #显示NlS域名